#my first python list
numbers = [45,67,34,76,23,45]

print(len(numbers))

sum(numbers)

print(sum(numbers))

# for i in range(len(numbers)):
#      print(numbers[i])

#  #list of methods
 
 #index
print(numbers.index(67))
 #after appending
numbers.append(90)
print(numbers)

 #before appending
numbers .append(100)
print(numbers.count(90))

 #sort-arrange the list in ascending order

for i in range(len(numbers)):
    print(numbers[i])

 #sort the list
 #numbers.sort(80)

 #count
 #element occurs in n list
print(numbers.count(90)) 


 #pop removes an element at a given index and returns that element
print(numbers.pop(0)) 


 #insert-insert an element  x at an index y and return that element
numbers.insert(2,100)

#for i in range(len(numbers)):
    #print(numbers[i])

 #remove -remove the first occurence in

#TIPS AD TRICKS

#multiplying a list
zero = [0] * 50
finallist = zero + numbers 
numbers[2:]
max(numbers)
min(numbers)
print(finallist)
print(zero)
print(numbers[2:])
print(max(numbers))

#CREATING A LIST
num=() #empty list

#for i in range(10):
    #insert elements into the list
    #num.append(i)

for i in range(len(num)):
    print(num[i])

    #EXERCISES
    #create a list of even numbers
    #even_num = []

for num in range(20):
    if(num % 2 == 0):
        even_nums.append(num)

for i in range(len(even_num)):
    print(even_num[1])

